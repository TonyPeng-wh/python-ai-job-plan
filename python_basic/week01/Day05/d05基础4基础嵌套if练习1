"""
独立练习：模型发布检查

AI 模型准备发布到生产环境前，需要依次检查：

自动化测试是否通过。
测试通过后，再检查人工审批是否通过。

输入规则：

1 表示通过
0 表示未通过
变量名：
test_is_passed
approval_is_passed
业务规则：

测试通过，并且审批通过：输出 允许发布模型
测试通过，但审批未通过：输出 等待人工审批
测试未通过：输出 测试未通过，禁止发布
最后始终输出 模型发布检查结束
"""

test_is_passed = int(input("自动化测试是否通过？请输入1或0："))

if test_is_passed == 1:
    approval_is_passed = int(input("人工审批是否通过？请输入1或0："))

    if approval_is_passed == 1:
        print("允许发布模型")
    else:
        print("等待人工审批")
else:
    print("测试未通过，禁止发布")

print("模型发布检查结束")

