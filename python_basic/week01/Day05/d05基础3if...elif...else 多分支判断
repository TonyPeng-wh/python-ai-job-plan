"""
if...elif...else 多分支判断
 一、本节目标

 学会处理三种或更多结果。

 真实工作中，模型响应时间不只是“合格”和“不合格”，还可能分为：

 优秀
 合格
 过慢

 这时使用：if...elif...else

 二、基本语法
if 条件1:
    条件1成立时执行
elif 条件2:
    条件1不成立、条件2成立时执行
else:
    前面的条件都不成立时执行

elif 可以理解为：

否则，如果……
"""

# 获取模型响应时间
response_time = float(input("请输入模型响应时间："))

if response_time <= 1.5:
    print("响应速度优秀。")
elif response_time <= 3.0:
    print("响应速度合格。")
else:
    print("响应速度过慢，需要优化。")

print("响应速度检查结束。")