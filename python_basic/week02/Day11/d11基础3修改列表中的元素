#   修改列表中的元素
#   列表中的元素不仅能读取，还可以直接被重新赋值。
answer_quality_scores = [0.86, 0.92, 0.78, 0.95]
#   语法：列表变量[索引]
answer_quality_scores[2] = 0.88
print(answer_quality_scores)

"""
字符串 str
→ 可以通过索引读取
→ 不能通过索引直接修改

列表 list
→ 可以通过索引读取
→ 也可以通过索引直接修改
这是 str 和 list 一个非常重要的区别。
"""

