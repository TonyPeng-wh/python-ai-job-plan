"""
d15函数7：函数外部变量
刚才我们知道：

函数里面创建的变量，通常只能在函数里面用。

那么反过来：

函数外面创建的变量，函数里面能不能用？
"""
system_name = "AI工单系统"  #外部变量 / 全局变量


def show_system():
    print(system_name)


show_system()
# 定义时没参数 → 调用时不用传值
# 定义时有参数 → 调用时要传值
# def 是创建函数，函数名() 是使用/执行函数。

"""
函数里面创建的变量
→ 外面通常不能直接用

函数外面创建的变量
→ 函数里面通常可以读取
"""