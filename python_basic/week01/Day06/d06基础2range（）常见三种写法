"""
range() 的三种常见写法
1. range(结束值)
写法：range(5)/产生：0、1、2、3、4
特点：
默认从 0 开始
不包含结束值 5
"""
# 示例1：
for task_number in range(5):
    print(task_number)

"""
2. range(开始值, 结束值)
写法：range(1, 5)/产生：1、2、3、4
特点：
从指定的开始值开始
不包含结束值

3. range(开始值, 结束值, 步长)
第三个参数表示每次增加多少。
例如：range(1, 10, 2)/产生：1、3、5、7、9
执行过程：
从1开始
每次增加2
到10之前停止

"""
#示例3：
for batch_number in range(1, 10, 2):
    print(f"正在抽查第{batch_number}批产品")

"""
统一规则：

包含开始值，不包含结束值，每次按照步长变化。

没有写开始值时，默认从 0 开始。

没有写步长时，默认每次增加 1。
"""